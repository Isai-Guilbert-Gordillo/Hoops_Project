node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite quedó reemplazado por Postgres; estos archivos ya no se usan
dev.db
prisma/dev.db
prisma/dev.db-journal

# Config local de Obsidian (notas personales, no es parte del proyecto)
.obsidian/
