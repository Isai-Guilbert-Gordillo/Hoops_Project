# Hello World Project

This is a simple "Hello World" project created using Node.js and Express.

## How to run

1. Make sure you have Node.js installed.
2. Run \`npm start\` or \`node index.js\` from the terminal.
3. Open a browser and navigate to \`http://localhost:3000\`.
